{
  "name": "otp-auth-backend",
  "version": "1.0.0",
  "description": "OTP Authentication Backend Service",
  "main": "Server.js",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production node Server.js",
    "dev": "nodemon Server.js",
    "build": "npm install --omit=dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "nodemailer": "^6.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}